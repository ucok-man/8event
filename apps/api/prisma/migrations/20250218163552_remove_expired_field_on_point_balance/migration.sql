/*
  Warnings:

  - You are about to drop the column `expiredAt` on the `PointBalance` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE "PointBalance" DROP COLUMN "expiredAt";
